# いい感じに割り勘

**気まずい割り勘を、いい感じに。**

上司は多め、遅刻・途中参加・お酒を飲まない人は少なめ。
飲み会の会計で面倒な「傾斜つき割り勘」を、スマホで30秒で決められるWebアプリです。

**入力は「金額」と「各レベルが何人か」だけ。** 参加者を1人ずつ並べる必要はありません。

- 合計金額は **必ず** お会計金額とぴったり一致します
- 100円単位などに丸めても一致します
- 入力内容は端末の中だけで計算し、サーバーには送信しません

---

## コンセプト

飲み会の最後、幹事が「会計42,800円。部長は多め、新卒は少なめ、1人遅刻、1人は飲んでない……」
という状況でその場で開き、説明を読まずに使えることを最優先にしています。

優先順位は以下の通りで、機能追加でUIが複雑になる場合は機能を削ります。

1. 直感的に使える
2. スマホで使いやすい
3. 結果が分かりやすい
4. 合計が必ず会計金額と一致する
5. 入力が面倒でない
6. プライバシー
7. 保守しやすいコード
8. 見た目
9. 機能数

---

## 技術スタック

| 項目 | 採用 |
| --- | --- |
| UI | React 18 |
| 言語 | TypeScript（strict） |
| ビルド | Vite 5 |
| スタイル | 素のCSS（1ファイル・CSS変数） |
| テスト | Vitest |
| ホスティング | Cloudflare Pages（静的） |

**依存は `react` / `react-dom` のみ。** 状態管理ライブラリ、UIライブラリ、アイコンライブラリ、Webフォント、
外部API、バックエンド、DB、ログインはいずれも使っていません。

---

## セットアップ

```bash
npm install     # 依存のインストール（Node.js 18以上）
npm run dev     # 開発サーバー（http://localhost:5173）
npm run test    # 割り勘計算ロジックのテスト
npm run build   # 型チェック + 本番ビルド（dist/ に出力）
npm run preview # ビルド結果の確認
```

補助コマンド

```bash
npm run typecheck   # tsc --noEmit のみ
npm run test:watch  # テストの監視実行
```

---

## ディレクトリ構成

```
.
├── index.html                 # title / meta description（SEOの基点）
├── public/
│   ├── favicon.svg            # 外部リクエストなしのSVGアイコン
│   ├── robots.txt
│   └── _headers               # Cloudflare Pages のセキュリティヘッダー
├── src/
│   ├── main.tsx               # エントリーポイント
│   ├── App.tsx                # 画面の組み立てのみ（ロジックを持たない）
│   ├── components/
│   │   ├── Hero.tsx           # ファーストビュー（3行だけ）
│   │   ├── AmountInput.tsx    # お会計金額（カンマ表示・数字キーボード）
│   │   ├── LevelCounters.tsx  # メイン入力：段階ごとの人数カウンター
│   │   ├── LevelSettings.tsx  # 「＋」で開く割合スライダーとメモ
│   │   ├── RoundingSelect.tsx
│   │   ├── FixedSection.tsx   # 折りたたみの固定金額設定
│   │   ├── SegmentedControl.tsx   # 汎用の1タップ切替
│   │   ├── ResultPanel.tsx        # 結果（レシート表示・コピー・共有）
│   │   └── Footer.tsx
│   ├── constants/
│   │   ├── app.ts             # アプリ名・キャッチコピー（改名はここだけ）
│   │   ├── split.ts           # 重み・理由・丸め単位などのドメイン定数
│   │   └── messages.ts        # 画面に出る日本語を集約
│   ├── hooks/
│   │   └── useSplitForm.ts    # 入力状態と操作
│   ├── lib/
│   │   ├── split.ts           # 割り勘計算（純粋関数・UI非依存）
│   │   ├── split.test.ts
│   │   ├── resultRows.ts      # 段階ごとに結果をまとめる表示ロジック
│   │   ├── resultRows.test.ts
│   │   ├── format.ts          # 金額の整形・入力パース
│   │   ├── shareText.ts       # コピー用テキスト生成
│   │   └── clipboard.ts       # コピー / Web Share（フォールバック込み）
│   ├── styles/
│   │   └── index.css
│   └── types/
│       └── split.ts           # ドメイン型
├── tsconfig.json
└── vite.config.ts
```

方針として、**UIと計算ロジックを完全に分離**しています。
`src/lib/split.ts` はReactに一切依存せず、入力を渡せば結果が返るだけの純粋関数です。

---

## 画面の作り

メイン画面は上から **お会計金額 → 各段階の人数 → 金額の単位 → 固定金額（折りたたみ）→ 計算ボタン**。
一般的なスマートフォンではスクロールなしでボタンまで届く高さに収めています。

```
[＋] 少なめ        [−] 0 人 [＋]
[＋] ちょい少なめ  [−] 0 人 [＋]
[＋] ふつう        [−] 4 人 [＋]
[＋] ちょい多め    [−] 0 人 [＋]
[＋] 多め          [−] 0 人 [＋]
```

左端の小さい「＋」を押すと、その段階だけの設定が開きます。

- **割合スライダー**（0.1〜3.0倍）。既定値に戻すリンク付き
- **メモ欄**（例:「田中、佐藤」）。計算には影響せず、コピーする文章に添えられます

右側の大きい「−／＋」は人数の増減です。役割が違うので大きさと枠線で区別しています。

### 固定金額

「この人は3,000円」と決めたい人だけを、計算ボタンのすぐ上の折りたたみに入れます。
人数カウンターとは独立していて、ここに並ぶのは固定金額の人だけです。
最初から1行あり、「＋」で増やせます。**金額が空の行は計算に入りません。**

### 結果の見せ方

人数で入力するアプリなので、結果も段階ごとにまとめて表示します。

```
少なめ            3,800円 / 3,700円
2人・山田、伊藤
ふつう                7,500円ずつ
2人・鈴木、高橋
ちょい多め            9,000円
1人・佐藤
多め                 11,300円
1人・部長
────────────────────────
合計                 42,800円  ✓ 合計ぴったりです
```

- 2人以上で金額が同じ行には「ずつ」を付け、1人あたりの金額だと分かるようにしています
- 丸めの都合で同じ段階の中に金額差が出た場合は、金額ごとに分けて表示します
- メモを入れた段階には、人数のあとにメモが並びます

「結果をコピー」ではLINEにそのまま貼れる文章を作ります。

```
🍻 今日のお会計
合計：42,800円

少なめ 2人：3,800円が1人、3,700円が1人（山田、伊藤）
ふつう 2人：7,500円ずつ（鈴木、高橋）
ちょい多め 1人：9,000円（佐藤）
多め 1人：11,300円（部長）

合計：42,800円

こちらの金額でよろしくお願いします！
```

末尾のひとことは既定でオン、チェックボックスで外せます。

---

## 割り勘計算ロジック

### 負担レベルと割合

ユーザーには5段階の日本語と「0.8倍」という表示しか見せませんが、
内部では **「ふつう = 100」を基準とした整数**で持っています（小数を使うと合計がずれるため）。

| 表示 | 既定の割合 | 内部の重み |
| --- | --- | --- |
| 少なめ | 0.5倍 | 50 |
| ちょい少なめ | 0.8倍 | 80 |
| ふつう | 1倍（基準） | 100 |
| ちょい多め | 1.2倍 | 120 |
| 多め | 1.5倍 | 150 |

**この割合はスライダーでユーザーが変更できます。**（0.1倍〜3.0倍、0.05刻み）
「ふつう」だけは基準なので固定です。金額は段階どうしの相対値だけで決まるため、
ほかの4段階を動かせばあらゆる配分を作れます。

### 計算順序

1. 会計総額を整数に正規化する（未入力・0円・負数・NaNはエラーとして扱う）
2. 固定金額の人を確定する（金額が空の行は「設定していない」とみなして無視）
3. 会計総額から固定金額の合計を引く（超過ならエラー）
4. 残額を各段階の割合に応じて分配する
5. 丸める
6. 丸めの差額を調整する
7. 合計が会計総額と一致することを保証する

### 丸めのアルゴリズム（最重要）

「先に分けてから丸める」と合計がずれます。そこで **先に丸め単位で刻んでから配ります。**

1. 残額を丸め単位の**かたまり**に分ける
   例）残額32,800円・100円単位 → 328個のかたまり + 端数0円
2. かたまりを割合に応じて配る（**最大剰余法**）
   - まず `かたまり数 × 割合 ÷ 割合合計` の整数部を配る
   - 余ったかたまりは「取りこぼしが大きい人 → 割合が大きい人 → 先に並んでいる人」の順に1個ずつ配る
   - この結果、同じ段階でも1人だけ1単位多く払うことがあります。合計を一致させるために
     避けられないので、結果画面では「3,400円が1人、3,300円が2人」と分けて表示します
   - この時点で全員の金額は必ず丸め単位の倍数になります
3. 丸め単位で割り切れない端数（例：100円単位で42,850円のときの50円）は
   **いちばん負担が重い人**がまとめて引き受けます
   - 全員に1円ずつ散らすより、人間の感覚として自然なため
   - 結果画面に「端数 +50円」と表示し、なぜ半端なのかが分かるようにしています
   - **固定金額の人は端数調整の対象外**です（本人が決めた金額を勝手に変えないため）
4. 最後に合計を検算し、万一ずれていたら重み参加者の先頭で吸収します（保険）

同じ入力なら常に同じ結果になります（乱数や実行順に依存しません）。

### エラーの扱い

計算ロジックはエラーコードだけを返し、日本語の文言は `constants/messages.ts` が持ちます。
技術的なメッセージは画面に出しません。

| 状況 | 画面に出る文言 |
| --- | --- |
| 会計金額が未入力・0円 | お会計金額を入れてください。 |
| 人数も固定金額も0 | 人数を1人以上にしてください。 |
| 固定金額の合計が超過 | 固定金額の合計が、お会計金額を◯◯円超えています。 |
| 残額があるのに人数が0人 | 残り◯◯円を払う人がいません。人数を1人以上にしてください。 |
| 丸め単位が大きすぎる | 金額の単位が大きすぎて、うまく分けられませんでした。単位を小さくしてください。（警告・計算は成立） |

そのほか、全角数字・カンマ・「円」などが混ざった入力、非常に大きい/小さい金額、
同じ名前、名前未入力、割合の異常値、入力途中の状態でもクラッシュしません。

---

## テスト

```bash
npm run test
```

`src/lib/split.test.ts`（計算）と `src/lib/resultRows.test.ts`（結果のまとめ方・コピー文）にあります。

1. 30,000円・3人・全員ふつう → 10,000円ずつ・合計一致
2. 多め / ふつう / 少なめ → 金額の大小関係と合計一致
3. 固定10,000円 + ふつう2人 → 合計一致
4. 42,800円・100円単位 → 全員100円単位・合計一致
5. 0円設定あり
6. 固定額の超過 / 固定額だけで一致 / 払う人がいない
7. 参加者1人
8. 丸めの端数（重い人へ寄る・固定額の人は対象外）
9. 非常に小さい金額 / 非常に大きい金額
10. 参加者30人
11. 5段階の金額の順序と、既定の割合（0.5 / 0.8 / 1 / 1.2 / 1.5 倍）
12. スライダーで割合を変えたときの挙動、「ふつう」が変更できないこと、範囲外の値
13. 金額が空の固定金額行が計算に入らないこと
14. 結果のまとめ方（段階ごと・端数で金額が分かれる・固定金額は末尾）
15. コピー文の「ずつ」「メモ」「ひとこと」

加えて、**800通りの組み合わせ（割合もランダム）を回して「合計＝会計金額」「金額は0以上の整数」を検証する不変条件テスト**を入れています。
まとめ表示をしても人数と合計が保たれることも同時に確認しています。
計算の中心はここなので、UIのテストより先にこちらを厚くしています。

> Vitestは `vite.config.ts` に数行足すだけで動くため、複雑性の増加はほぼありません。
> DOMを使わない純粋関数だけをテストしているので、jsdom等の追加も不要です。

---

## プライバシー設計

- 会計金額・名前・役職・遅刻や飲酒などの設定は**すべてブラウザ内で処理**します
- サーバーへの送信、外部API、アクセス解析、Cookieは**ありません**
- 例外は「共有」ボタンだけで、これはOSの共有機能に本文を渡すユーザー操作です（画面にも明記）
- V1では **localStorageにも保存しません**

### なぜV1はlocalStorageなしか

保存すればリロードで消えない利点はありますが、

- 飲み会の会計は基本的に一発勝負で、次回に前回の名前が残っていると混乱・気まずさの原因になる
- 端末を他人に見せる／貸す場面が多く、参加者名が残るのは望ましくない
- 「保存していません」と言い切れる方が、初見のユーザーに説明しやすい
- 実装と状態管理がシンプルになる

以上から、V1は保存なしを選びました。
将来入れる場合は「保存する」を明示的にオンにするオプトイン方式を推奨します。

### セキュリティ

- `dangerouslySetInnerHTML` は使用していません。ユーザー入力はすべてテキストとして描画します
- APIキー・秘密情報は扱いません
- `public/_headers` で `X-Content-Type-Options` / `X-Frame-Options` / `Referrer-Policy` / `Permissions-Policy` を設定しています
- CSPは意図的に未設定です。ビルド構成や将来の広告タグと衝突しやすいため、
  公開後に実際の配信内容を見てから追加してください

---

## Cloudflare Pages へのデプロイ

1. GitHubにリポジトリを作成し、push する
2. Cloudflare Dashboard → Workers & Pages → Create → Pages → Connect to Git
3. ビルド設定

   | 項目 | 値 |
   | --- | --- |
   | Framework preset | Vite |
   | Build command | `npm run build` |
   | Build output directory | `dist` |
   | Root directory | （リポジトリ直下ならそのまま） |
   | Node version | 環境変数 `NODE_VERSION` に `20` を設定 |

4. Save and Deploy

SPAルーティングは使っていないため `_redirects` は不要です。
将来ページを増やす場合は、ルーターではなく **Viteのマルチページ構成（`/how-to/index.html` など）** を推奨します。
各ページが独立したHTMLになるため、SEOでもパフォーマンスでも有利です。

独自ドメインは Pages の Custom domains から設定し、その後 `index.html` の
`<link rel="canonical">` と OGP を実ドメインに更新してください。

---

## 今後の予定

### V1.1 候補

- `/how-to`・`/faq`・`/privacy`・`/terms` などの静的ページ追加（マルチページ構成）
- 結果のURL共有・QRコード
- 「幹事を少なめ」などの条件プリセット
- 遅刻時間や途中参加時間から傾斜を自動提案
- ダークモード / PWA
- 履歴・よく使うメンバーの保存（オプトイン）

### 見送った機能：段階そのものの追加

「少なめ〜多め以外の段階も設定から追加したい」という案は、V1では見送りました。

- メイン画面の5行はすでに縦幅の大半を使っていて、行が増えると
  「計算ボタンまでスクロールなし」という一番大事な性質が崩れる
- 割合スライダーを入れたことで、段階を増やさなくても
  「少なめ＝0.3倍」のように好きな配分を作れる

将来入れるなら、段階の追加ではなく **保存できる条件プリセット**（「うちの部署の飲み会」など）
のほうが、画面を増やさずに同じ目的を達成できると考えています。

### 広告について

V1では実装していません。追加する場合は以下を前提にしてください。

- 掲載位置は**結果の下（フッターの直前）**。`App.tsx` に該当箇所のコメントがあります
- 計算ボタン・結果の操作ボタンの近くには置かない（誤タップ防止）
- 枠の高さを固定してCLSを抑える
- 広告ありきでレイアウトを変えない

---

## ライセンス / 注意

本アプリの計算結果はあくまで目安です。実際の支払いは参加者間で確認してください。
